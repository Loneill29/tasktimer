<!DOCTYPE html>
<html>
  <head>
    <title>TaskTimer</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag "/assets/stylesheets/users" %>
    <%= stylesheet_link_tag "/assets/stylesheets/items" %>
    <link href="https://fonts.googleapis.com/css?family=Raleway:400,600" rel="stylesheet">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <% if user_signed_in? %>
    <div id="current-user">
      Signed in as: <strong><%= current_user.email %></strong> |
      <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link'  %> -
      <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
    <% end %>
    </div>
    <div class="container">
      <ul class="nav nav-tabs">
        <li><%= link_to "My Tasks", user_session_path, :onclick => "blocmetrics.report('my to-do list link clicked')" %></li>
      </ul>
      <p class="notice"><%= notice%></p>
      <p class="alert"><%= alert %><p/>

    <%= yield %>
  </body>
</html>
